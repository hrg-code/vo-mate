# douyin_video_asr_results 字段说明：7585913482206383412

本文档基于 MongoDB `crawler_raw_db.douyin_video_asr_results` 中 `video_id = "7585913482206383412"` 的实际文档生成，用来说明这条抖音视频 ASR 语音识别结果中各字段的含义。

## 数据概览

- 视频 ID：`7585913482206383412`
- 识别语言：`zh`
- 视频/音频时长：`60.0` 秒
- 分段数量：`21`
- 创建时间：`2026-03-18 08:45:30.808000`
- 更新时间：`2026-03-18 08:45:30.808000`

## 顶层模块

| 字段 | 类型 | 示例/结构 | 字段含义 |
| --- | --- | --- | --- |
| `_id` | `ObjectId` | `69ba662af6e9139651db36ce` | MongoDB 自动生成的文档主键，用于唯一标识这条 ASR 结果记录。 |
| `video_id` | `str` | `7585913482206383412` | 抖音视频 ID，用于关联 `douyin_video_raw` 等视频主表中的同一作品。 |
| `text` | `str` | `程序员能干一辈子吗？我觉得大部分人应该是不可以的...` | ASR 识别出的完整口播文本，把所有分段内容合并成一段连续文本。 |
| `language` | `str` | `zh` | ASR 判断或配置的语言代码；`zh` 表示中文。 |
| `segments` | `array` | `21` 项；首项为对象 | 按时间切分后的字幕/转写片段列表，每一项包含起止时间和该时间段内的文本。 |
| `duration_seconds` | `float` | `60.0` | ASR 处理的视频或音频总时长，单位秒。 |
| `created_at` | `datetime` | `2026-03-18 08:45:30.808000` | 这条 ASR 结果首次写入数据库的时间。 |
| `updated_at` | `datetime` | `2026-03-18 08:45:30.808000` | 这条 ASR 结果最近一次更新的时间。 |

## segments 字段结构

`segments` 是字幕时间轴数组。每个元素代表一小段语音识别结果：

| 字段路径 | 类型 | 示例 | 字段含义 |
| --- | --- | --- | --- |
| `segments[].start` | `float` | `0.0` | 当前语音片段的开始时间，单位秒，从视频开头开始计时。 |
| `segments[].end` | `float` | `2.19` | 当前语音片段的结束时间，单位秒。 |
| `segments[].text` | `str` | `程序员能干一辈子吗？` | 当前时间片段内识别出的文本内容。 |

## 字段关系

| 关系 | 说明 |
| --- | --- |
| `video_id` -> `douyin_video_raw._id` | 可用 `video_id` 关联原始视频文档，例如本条对应 `douyin_video_raw._id = "7585913482206383412"`。 |
| `text` <- `segments[].text` | `text` 通常是所有 `segments[].text` 按时间顺序拼接后的完整文本。 |
| `duration_seconds` 与 `segments[-1].end` | `duration_seconds` 表示总时长；最后一个 segment 的 `end` 通常接近该值，但可能因 ASR 切分或静音片段略有差异。 |

## 使用提示

- 做全文搜索、主题提取、关键词分析时，优先使用 `text`。
- 做字幕、逐句回放、口播节奏分析时，优先使用 `segments`。
- `segments[].start` 和 `segments[].end` 是秒级浮点数，可以直接用于生成 SRT/VTT 字幕。
- `video_id` 建议按字符串处理，避免大整数 ID 在不同语言或前端环境中出现精度损失。
